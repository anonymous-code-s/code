# Direct dependency versions recorded for the reference paper environment.
# This is not a full transitive lock file; retain `pip freeze` with each final run as well.
amrlib==0.8.0
FlagEmbedding==1.3.5
numpy==1.26.4
openai==1.99.9
penman==1.3.1
python-dotenv==1.1.1
PyYAML==6.0.2
sentence-transformers==5.1.0
